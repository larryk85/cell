#ifndef @NAMESPACE@_VERSION_PP_

// These preprocessor definitions are generated by `CMake` or `generate_version_info.py`
// The `NAMESPACE` parts will be the changed to the "namespace" that you specify in CMake or the python script

/**
 * @def @NAMESPACE@_NAMESPACE
 * @brief The namespace used in the library.
 */

/**
 * @def @NAMESPACE@_NAMESPACE_X(NM)
 * @brief Macro to concatenate the namespace with another name.
 * @param NM The name to concatenate with the namespace.
 */

/**
 * @def @NAMESPACE@_MAJOR
 * @brief The major version number of the library.
 */

/**
 * @def @NAMESPACE@_MINOR
 * @brief The minor version number of the library.
 */

/**
 * @def @NAMESPACE@_PATCH
 * @brief The patch version number of the library.
 */

/**
 * @def @NAMESPACE@_TWEAK
 * @brief The tweak version number of the library.
 */

/**
 * @def @NAMESPACE@_USE_SUFFIX
 * @brief Flag indicating whether a suffix is used in the version number.
 */

/**
 * @def @NAMESPACE@_USE_GIT_HASH
 * @brief Flag indicating whether the git hash is used in the version number.
 */

/**
 * @def @NAMESPACE@_SUFFIX
 * @brief The suffix used in the version number.
 */

/**
 * @def @NAMESPACE@_GIT_HASH
 * @brief The git hash of the library.
 */

/**
 * @def @NAMESPACE@_VERSION_STRING
 * @brief The version string of the library, including the major, minor, patch, tweak, suffix, and git hash.
 */

#define @NAMESPACE@_NAMESPACE       @NAMESPACE@  /**< The namespace used in the library. */
#define @NAMESPACE@_MAJOR           @MAJOR@      /**< The major version number of the library. */
#define @NAMESPACE@_MINOR           @MINOR@      /**< The minor version number of the library. */
#define @NAMESPACE@_PATCH           @PATCH@      /**< The patch version number of the library. */
#define @NAMESPACE@_TWEAK           @TWEAK@      /**< The tweak version number of the library. */
#define @NAMESPACE@_SUFFIX          "@SUFFIX@"   /**< The suffix used in the version number. */
#define @NAMESPACE@_GIT_HASH        "@GIT_HASH@" /**< The git hash of the library. */

#define @NAMESPACE@_CREATE_INFO(NM) \
   versa::info::version_info NM = {@NAMESPACE@_MAJOR, @NAMESPACE@_MINOR, @NAMESPACE@_PATCH, @NAMESPACE@_TWEAK, @NAMESPACE@_SUFFIX, @NAMESPACE@_GIT_HASH};

#endif // @NAMESPACE@_VERSION_PP_