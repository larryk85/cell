set(PACKAGE_VERSION @astronaught_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@astronaught_VERSION_MAJOR@")
   if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@astronaught_VERSION_MINOR@")
      set(PACKAGE_VERSION_MATCH TRUE)
   elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@astronaught_VERSION_MINOR@")
      set(PACKAGE_VERSION_COMPATIBLE TRUE)
   else()
      set(PACKAGE_VERSION_UNSUITABLE TRUE)
   endif()
else()
   set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
